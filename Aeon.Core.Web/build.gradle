dependencies {
    compile project(':Aeon.Core')
}

publishing {
    publications {
        mavenJava(MavenPublication) {
            from components.java
            artifactId 'aeon.core.web'
        }
    }
}